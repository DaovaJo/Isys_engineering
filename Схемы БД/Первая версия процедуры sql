CREATE PROCEDURE users.adding
@ USER
@ PASSWORD
AS
BEGIN
SET NOCOUNT ON 

     INSERT INTO users.adding
          (                    
            USER,
            PASSWORD
          ) 
     VALUES 
          ( 
@ USER, 
@ PASSWORD
          ) 
END


exec users.adding
@ USER = ‘’, 
@ PASSWORD = ‘’

